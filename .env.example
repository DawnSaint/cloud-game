# MongoDB Atlas connection string
# Replace <db_username> / <db_password> with your Atlas database user credentials.
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@mongodb.net

# JWT secret used to sign auth tokens
JWT_SECRET=cloud-game-dev-secret
